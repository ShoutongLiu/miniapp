<view>
    <block wx:for="{{blogList}}" wx:key="_id">
        <view class="blog-panel">
            <blog-list blog="{{item}}" bindtap="goDetail" data-blogid="{{item._id}}" />
            <blog-ctrl blogId="{{item._id}}" blog="{{item}}"></blog-ctrl>
        </view>
    </block>
</view>